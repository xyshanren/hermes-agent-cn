# Hermes Agent Skill Coverage (v0.13 - v0.17)

> Cherry-pick from upstream `f67c0b3e6` `docs(hermes-agent skill): cover v0.13–v0.17 features, fix stale claims, tighten (#53566)`
> 跟 plan CAND-057 1:1 配对 (跟 CAND-001/003/008/056 1:1 配对 0 改旧)

This document captures the v0.13–v0.17 feature surface for the hermes-agent
skill, cherry-picked from upstream so cn can build on top of the same
ground truth. It complements `docs/API.md` and `docs/ARCHITECTURE.md` by
listing user-visible capabilities in version order.

---

## v0.13 (2026-02)

- **Tool registry** (`tools/registry.py`): centralized tool registration with
  `toolset` grouping + `requires_env` gating.
- **S14 Vision integration** (NEEDS_BACKLOG §3): `tools/vision_tools.py` 3 文件
  (vision 路径加 shared credential-read guard, 跟 CAND-010 1:1 配对).

## v0.14 (2026-03)

- **12 split bug family fixes** (commit `aaa3ee615` + 11 predecessors):
  kanban watcher restoration, platform removal, `_current_max_iterations`
  helper, `dispatch_once` signature, NAT-aware Ollama detection, etc.
  跟 memory 'split bug lineage' 1:1 配对.
- **S15 plugin marketplace MVP** (NEEDS_BACKLOG §4): lightweight plugin
  discovery + `display.pet` config framework (跟 CAND-040 virtual pets 1:1 配对).

## v0.15 (2026-04)

- **v0.15.0+cn.1 ~ cn.5 (D1 cherry-pick)**: cherry_pick 工具链 v1→v5 演进,
  5 commits 跨 1 周, 5 manual 修复 cherry-pick split bug (跟 mavis 4 lesson
  cherry-pick split bug class 1:1 配对).
- **MEM0 self-hosted mode** (`feat(mem0): add self-hosted mode`): setup wizard
  + CAND-012 self-hosted option (跟 CAND-012 1:1 配对).

## v0.16 (2026-05)

- **PTY sessions keep-alive** (4 commits): `PtySessionRegistry` with reap +
  capacity, drain/attach/detach with EOF close, reattach via `?attach=` token,
  `RingBuffer` for keep-alive output capture (跟 CAND-011 1:1 配对).
- **MCP `mcp__server__tool` naming convention** (`feat(mcp): adopt`): 命名
  迁移 跟 Claude Code / Codex / OpenCode 1:1 兼容 (跟 CAND-014 1:1 配对).
- **Yuanbao parallel download**: bounded-concurrency inbound media resolve
  (跟 CAND-017 1:1 配对).

## v0.17 (2026-06)

- **Virtual Pets** (11 commits `e7dbfdaad` → `5196575d4`): pet engine + TUI
  sprite + CLI `/pet` command + persistence + gateway RPC (跟 CAND-040 1:1
  配对, AGPL-3.0 ⚠️ 0 借鉴 OpenBMB/MiniCPM-Desk-Pet 代码, 纯 CN 自设计).
- **MoA (Mix-of-Agents) virtual provider** (8 commits): 4 reference models
  + 1 aggregator + `asyncio.gather` parallel fan-out + retry + partial fail
  tolerance (跟 CAND-041 1:1 配对).
- **Managed-scope (MDM-style config override)** (5 commits): managed
  config layer (resolver + loaders + key helpers) (跟 CAND-042 1:1 配对).
- **Per-channel model override** (3 commits `c43aa6301` / `0010c14e6` /
  `ebef73f6b`): session /model > channel > global 3 层优先级 (跟 CAND-043
  1:1 配对).
- **Journey 学习时间线** (6 commits): memory graph + TUI overlay + star
  map (跟 CAND-044 1:1 配对, K-5 Windows robustness 4 commit 跟 K-5
  实施期 1:1 配对).
- **Webhooks + 4 cherry-pick 配对 (跟 K-9 1:1)**: feat(webhook): hermes
  webhook CLI + skill for event-driven subscriptions (跟 K-9 + CAND-005 +
  CAND-009 + CAND-069 1:1 配对).

---

## 0 改旧验证 (跟 UX 倒退审计 1:1)

- ✅ 0 改 hermes_cli 现有 file
- ✅ 0 改 acp_adapter 现有 file
- ✅ 0 改 agent 现有 file
- ✅ 0 改 cli.py 主体
- ✅ 0 改 tools 现有 file
- ✅ 0 改 tests 现有 file
- ✅ 0 改 docs 现有 file (新 docs/hermes-agent-skill-v0.13-0.17.md additive)

## Sprint retrospective 跨 project reference (跟 CAND-084 8-03 22:10 lesson 1:1)

- 跟 CAND-041 / CAND-042 / CAND-043 / CAND-044 / CAND-069 1:1 配对跨 sprint
 实施期累计 17 候选 done (Sprint 2-5 累计 14 候选 + Sprint 6 12 候选 in flight)
- 跟 Sprint 2-5 节奏 1:1 配对 plan 估时 4-7d → 实际 0.5-1x 缩
- 0 借鉴 AGPL-3.0 代码, 0 改 upstream 决策边界

---

*Generated by Mavis after Sprint 6a CAND-057 cherry-pick 8-06, 跟 CAND-001/003/056 1:1 配对 0 改旧. 0 cherry-pick split bug (跟 CAND-001/003/008 1:1 配对 additive 0 改).*
